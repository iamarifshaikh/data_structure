def strandSort(array):
    output = [] # For storing the sorted array
    strand = [] # For storing the first element of the array and compare it against the other elements
    
    strand.append(array[0])
    
if __name__ == "__main__":
    array = [24, 35, 82, -3, -9, 14, 10]
    strandSort(array)